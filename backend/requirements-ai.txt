# AI and LLM dependencies (optional - only install if RAG_ENABLED=true)
# These packages are heavy and slow to install, so they're separated

# OpenAI client for API calls
openai==1.3.7

# Vector database for RAG
chromadb==0.4.15

# Sentence transformers for embeddings
sentence-transformers==2.7.0

# PyTorch (required by sentence-transformers)
torch==2.3.1

# BM25 ranking for text search
rank-bm25==0.2.2

# Tokenizer for OpenAI models
tiktoken==0.7.0
